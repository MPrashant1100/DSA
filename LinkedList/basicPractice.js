
class Node  {
    constructor(data) {    // it takes data as a parameter, and when a Node object is created
        this.data = data;  // data - to store the value
        this.next = null   // next - to store the reference to the next node in the list.
    }
}
const basicPractice = () => {

}